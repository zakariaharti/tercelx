const initState = {
  message: 'hello world'
};

const AppReducer = (state: any = initState, action: any) => {
  switch(action.type){
    default:
     return state;
  }
};

export default AppReducer;
